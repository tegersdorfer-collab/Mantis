# Schlanke Abhängigkeiten für CI/Tests.
# Die Tests mocken DB, Netzwerk und LLMs; die schweren Voice-/ML-Pakete (piper-tts,
# pywhispercpp, openwakeword, onnxruntime) werden von ihren Modulen LAZY geladen
# (Import erst im Funktionskörper), daher braucht die Test-Collection sie nicht.
# Hier steht nur, was auf Modulebene über die getesteten Module importiert wird.
psycopg2-binary>=2.9
pgvector>=0.3.0
numpy>=1.26
httpx>=0.27
fastapi>=0.110
python-multipart>=0.0.9
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
ollama>=0.3.0
anthropic>=0.40.0
jevkit @ git+https://github.com/tegersdorfer-collab/jevkit.git@v0.1.0
bleak>=3.0.0
icalendar>=5.0
python-telegram-bot>=21.0
psutil>=5.9
feedparser>=6.0
pytest>=8.0
