# LLM
ollama>=0.3.0
anthropic>=0.40.0
jevkit @ file:///Users/timoegersdorfer/jevkit

# Telegram
python-telegram-bot>=21.0

# Kalender (ICS-Parsing in domains/calendar.py)
icalendar>=5.0

# Database
psycopg2-binary>=2.9
pgvector>=0.3.0

# Web Search & URL extraction
duckduckgo-search>=6.0
yt-dlp>=2024.1.0
trafilatura>=1.9.0

# Dashboard
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
websockets>=13.0

# Config validation
pydantic-settings>=2.0

# TTS (Text-to-Speech, lokal)
piper-tts>=1.4.0

# Utilities
python-dotenv>=1.0
asyncio-throttle>=1.0
psutil>=5.9
httpx>=0.27
pywhispercpp>=1.5.0
numpy>=1.26
onnxruntime>=1.17

# Google Calendar API (bidirektionale Kalendersynchronisation)
google-api-python-client>=2.120
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

openwakeword>=0.6.0

# Robotik — BLE-Steuerung X5-Droid (Clementoni RoboMaker)
bleak>=3.0.0

# UI-Automatik — macOS Accessibility (App-Fernsteuerung). Nur macOS; zieht pyobjc.
# Lazy importiert (tools/uiauto/engine.py) → Mantis startet auch ohne das Paket.
# Plattform-Marker: pyobjc/atomacos gibt es nur auf macOS (sonst bricht pip auf Linux/CI).
atomacos>=3.3.0; sys_platform == "darwin"

# News-Globus — RSS/Atom-Parsing
feedparser>=6.0
